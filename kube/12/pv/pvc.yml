kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-web
spec:
  resources:
    requests:
      storage:  2Gi
  accessModes:
  - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: manual